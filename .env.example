# Bask's local time zone, used by Python and notification timestamps.
TZ=America/New_York

# Host port for the dashboard.
BASK_PORT=8080

# Persistent settings and SQLite history directory.
BASK_DATA_PATH=./data

# Keep enabled on a Raspberry Pi. Set false for web-only development/testing.
BASK_SCANNER_ENABLED=true
