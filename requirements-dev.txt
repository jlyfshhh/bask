# Test-only dependencies. Deliberately separate from requirements.txt so they
# are not installed into the published image.
#
# Starlette 1.x prefers the httpx2 compatibility package for TestClient and
# only keeps legacy httpx support as a deprecated fallback. Without it the API
# tests either warn today or stop collecting once that fallback is removed.
httpx2>=2.12.0
